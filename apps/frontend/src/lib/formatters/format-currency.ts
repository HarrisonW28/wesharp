/**
 * Re-exports — prefer importing from `@/lib/format/money` (`formatGBP`, `formatGBPMajor`).
 */

export { formatGBP as formatCurrencyMinor, formatGBPMajor as formatCurrency } from "@/lib/format/money";
