/**
 * Outer width + horizontal padding for public marketing content (matches home page sections and hero).
 */
export const PUBLIC_SITE_CONTENT_CONTAINER_CLASS = "mx-auto max-w-7xl px-4 md:px-8";

/** Desktop nav mega-menu: same max line length as `PUBLIC_SITE_CONTENT_CONTAINER_CLASS` inner width. */
export const PUBLIC_SITE_NAV_DROPDOWN_WIDTH_CLASS =
  "w-[min(80rem,calc(100vw-2rem))] max-w-[min(80rem,calc(100vw-2rem))]";
